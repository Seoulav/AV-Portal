# W-20260925-005 Work 인계

- 작업: 공개 화면 시스템 버전 표기
- 승인: 2026-09-25 사용자 직접 지시
- 구현 브랜치: `codex/W-20260925-005-system-version`
- 기준 main: `c954fb41b5c03a1beafe77f084e2c71dd129d2eb`

## 구현 범위

- 공개 Library와 공통 Product Detail 우측 하단에 같은 시스템 버전 배지를 표시한다.
- 표시 형식은 `SYSTEM v<package version> · build <Actions run number> · <commit SHA 7자리>`이다.
- GitHub Pages workflow가 배포 직전에 `beta/site/version.json`을 생성하므로 배포마다 build와 revision이 자동 갱신된다.
- 제품 JSON, 이미지, 문서, 사양, I/O 및 검증 상태는 변경하지 않는다.

## 검증 결과

- 메타데이터 포맷과 홈·상세 연결을 `tests/system-version.test.mjs`에서 검사한다.
- `beta/verify-pages.mjs`가 버전 자산과 두 화면의 표시 마커를 검사한다.
- `npm test`: 71 pass·1 skip·0 fail. Pages 아티팩트, 메타데이터 생성, JavaScript 구문, 정본/생성물 동기화와 `git diff --check`가 통과했다.
- 로컬 홈과 DM7 상세에서 배지가 우측 하단에 표시되고 제품 이미지·탐색 조작을 막지 않는 것을 확인했다.

## 다음 Work 입력

- 이후 공개 UI 작업에서도 `system-version.js`, `system-version.css`, `version.json` 연결을 유지한다.
- package version 자체를 올리는 정책은 별도 릴리스 정책으로 정하며, 이번 작업은 기존 `0.1.0`을 유지하고 배포 build와 revision만 자동 갱신한다.
