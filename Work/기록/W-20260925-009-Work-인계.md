# W-20260925-009 → Work 인계

- 인계 작성일: 2026-09-25
- 구현 PR·상태: (곧 생성) REVIEW
- 기준 main: `d9a955f`
- 관련 명세·진행 기록: [Work/작업/W-20260925-009.md](../작업/W-20260925-009.md), [Work/지시서.md](../지시서.md) "W-20260925-009 — 조사 완료 장비 목록 등재"
- 자료 접근 범위: 이번 작업의 입력은 전부 GitHub에서 읽을 수 있다(`docs/research/equipment-listing-decisions-2026-09-25.json`). 로컬 전용 원본 워크북은 참조하지 않았다.

## 실제 완료·검증

`beta/site/catalog.json`을 27개 → **174개**로 늘렸다(명세 목표 183개에서 -9). 결정표 A등급 177행 중 제조사 미특정 2건, 기존 27개와 중복 19건, `official_urls`가 PDF 문서 하나뿐인 9건을 제외한 **147건**을 추가했다. 상세 페이지·검증 로직·이미지 매니페스트는 변경하지 않았다.

검증: `node beta/verify-pages.mjs` → `174 equipment items, 5 details, 13 reviewed official WebP images, no PDF binaries.` `npm test` 87 pass·1 skip·0 fail. 기존 27개는 병합 전과 완전히 동일함을 확인했다. `beta/verify-pages.mjs` 자체는 한 줄도 고치지 않았다(제공된 지시대로).

Chromium으로 1440px·390px 확인: 가로 넘침 0, 콘솔 오류 0, 카테고리 카드 6개 중 5개가 0 초과, 신규 항목 카드가 "공식 링크 확인" 상태와 제조사 공식 페이지 링크를 정상 표시.

세부 내용은 `Work/지시서.md`의 "W-20260925-009" 항목에 전부 있다. 요약하면 다음 세 가지를 발견해 기록했다:

1. **PDF만 있는 URL 9건** — 결정표에 대체 URL이 없어 이번 배치에서 제외. 결정표에는 여전히 A등급으로 남아 있다.
2. **`tests/readable-catalog.test.mjs`의 사전 버그** — `&` 포함 URL(신규 12건)에서 HTML 이스케이프 비교가 틀렸던 것을 발견해 최소 수정했다(`escapeHtml` export + 테스트가 이스케이프 후 비교). `beta/verify-pages.mjs`는 건드리지 않았다.
3. **`전원·인프라` 카테고리 카드가 이미 비어 있었다** — 기존 27개 상태에서도 0개였던 사전 상태. 이번 작업이 만든 문제가 아니다.

## 남은 일과 다음 작업 후보

우선순위 순.

1. **PDF만 존재하는 9건 처리 방침 결정** (의존성: 없음). 두 가지 경로가 있다 — (a) 새 조사로 제조사 HTML 제품 페이지를 다시 찾는다 (Lightware HDMI20-OPTJ 시리즈, Ross Video ULTRITOUCH-PS·SFP-MADI-COAX, Shure UA845UWB, NETGEAR GS728TPP·GS116PP·GS108PP, Logitech Rally Mic Pod), (b) PDF만 있는 경우를 위한 별도 필드·정책을 스키마에 추가한다(Product Data Schema 변경이라 사용자 승인 필요). 최소 범위는 (a)이고 9건이므로 작지만, "새 조사 금지" 원칙과 충돌하므로 명시적 승인이 필요하다.
2. **B등급 79건(계열 페이지) 공개 여부** — W-20260925-009 명세의 미결 사항에 이미 기록됨. 카탈로그에 링크 성격 필드(예: `official_link_kind: "exact" | "family"`)를 추가하는 Product Data Schema 변경이 선행돼야 한다. 사용자 승인 필요.
3. **RTCOM 65행** — AGENTS.md 정책상 제조사 직접 제공 자료로만 다뤄야 한다. 별도 작업.
4. **C등급 12건** — 모델 자체가 불확정. 공개 대상 아님.
5. **카테고리 3단 계층 통일(W-20260924-011)**과 **`전원·인프라` 카드 공백**은 이번 작업과 독립된 후속 과제.

## Work가 할 일

1. 최신 main, 이 PR, 명세, 이 인계서와 `Work/지시서.md`를 대조한다.
2. PDF 9건·B등급 79건에 대한 다음 작업은 새 ID의 DRAFT 명세로 작성한다. 특히 B등급 공개는 Product Data Schema 변경을 동반하므로 사용자 승인 없이 READY로 만들지 않는다.
3. 이번 PR의 `tests/readable-catalog.test.mjs` 수정(`escapeHtml` export 경유 비교)이 명세의 "건드리지 말 것" 범위 해석과 맞는지 확인한다. 범위를 벗어났다고 판단되면 되돌리고 별도 버그 수정 작업으로 분리한다.

## Work가 사용자에게 돌려줄 내용

- **완료**: 27 → 174개 (계획 183에서 9건 보류)
- **PR**: (아래 생성)
- **결정 필요**: PDF만 있는 9건을 어떻게 할지(새 조사 승인 또는 스키마 확장), B등급 79건 공개 여부(스키마 확장 승인)
- **공개 직전 검증**: 아직 수행 전. Opus가 별도로 진행 예정
