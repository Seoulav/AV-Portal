# W-20260924-009 → Work 인계 — 공개 상세 진입·해시·CI

- 기준 main: `d782d4e585be57ed42801fd4cba95b02cd96348f`
- 명세: [W-20260924-009](../작업/W-20260924-009.md). 착수 시 Draft [PR #45](https://github.com/Seoulav/AV-Portal/pull/45) head `2cc114983daa07814bf736175a389eedd4d824c9`에서 읽었고, 진행 중 READY로 갱신된 PR #45가 `c8c4bc217c3f4c5de4c5e8e5d605d197c3aabd4f`로 main에 병합되어 이를 구현 브랜치에 통합했다.
- 구현 브랜치: `codex/W-20260924-009-detail-entry`; 사용자 2026-09-24 지시로 구현·검증·PR까지 승인. **main 병합·Pages 재배포는 별도 승인 대기.**

## 구현 결과

- 공개 상세에서 product 값이 없거나 비면 Library로 이동한다. 잘못된 값·형식의 오류 안내에는 Library 링크가 있다. 공개본에 없는 `content.json` fallback은 제거했다.
- 본문 생성 뒤 초기 해시 위치와 현재 섹션 표시를 맞춘다. 브라우저 새로고침 스크롤 복원과 충돌하지 않도록 처리했고, 출처 앵커가 sticky 내비게이션에 가리지 않게 했다. 잘못된 퍼센트 인코딩 해시는 예외 없이 무시한다.
- 상세 HTML의 CSS·JS와 모델 모듈 import에는 동일한 변경 커밋 기준 `?v=ae4681b`를 넣었다. Pages workflow는 공개 아티팩트 검사 후 `npm test`가 통과해야만 기존 `beta/site` 업로드 단계로 진행한다.
- 다섯 공개 상세의 콘텐츠, Library 데이터, prototype, 공개 파일 허용 목록·고정 해시, 외부 도구 링크 및 검증·권한 상태는 변경하지 않았다.

## 검증과 한계

- `node beta/verify-pages.mjs` 통과. `npm test`는 42 pass / 1 skip(로컬 비공개 원본 목록 경로 없음) / 0 fail. 로컬 정적 서버와 Chromium에서 Desktop 1440px·Mobile 390px을 직접 확인했다.
- BRC-AM7 `#io` 직접 진입·새로고침 제목 상단은 1440px +100.406px, 390px +100.391px. `#source-HS`는 각각 +342.156px(문서 끝 스크롤 한계), +114.844px로 가려지지 않았다. 여섯 탭의 제목 상단은 약 +100px이고 활성 표시가 일치한다.
- 상세 다섯 개의 Quick Documents 4칸, Features 8/8/8/7/8, Specs 27/16/16/13/16, I/O 14/15/11/4/9, I/O 상시 표시 및 수평 넘침 0을 확인했다. Library 장비 27·서비스 0·상세 링크 5개를 확인했다.
- 이번 변경은 아직 공개되지 않았다. 실제 공개 URL 재검증과 Pages Actions 결과는 별도 병합·배포 승인 후 수행해야 한다. 비공개 `hkkim/`·`outputs/` 자료는 필요하지 않았고 PR에 넣지 않는다.
- 독립 코드 리뷰의 뒤로 가기 지적을 반영해 해시 없는 상세의 브라우저 스크롤 복원(4000px → Back 4000px)을 유지했다. 해시가 있는 상세에서 Back으로 이전 읽던 위치 대신 해시 근처로 가는 현상은 기준 main에서도 재현되어 별도 후속 UX 검토로 남긴다. 이번 해시 보정은 직접 진입과 새로고침에만 적용한다.

## Work의 다음 입력

- Work는 PR #45의 READY 병합과 이번 구현 PR의 의존 관계를 확인한다. READY 문서에는 조건부 배포 승인 기록이 있으나, 이 채팅의 직접 지시는 main 병합과 Pages 재배포를 별도 승인 후로 제한한다. 이번 구현 PR은 그 승인 전까지 병합하지 않는다.
- 공개 배포 승인 후 Codex가 Pages 실행 결과, 실제 공개 URL의 빈/잘못된 product·해시 직접 진입·새로고침·1440px/390px 결과를 기록한다.
- 별도 작업 후보: 상태 배지 명암비·작은 글씨, 내부 링크의 새 탭 열기, 표시 텍스트에 의존하는 Library 상세 연결, Library 자산 캐시 버전. 이 항목들은 W-009 범위 밖이다.
