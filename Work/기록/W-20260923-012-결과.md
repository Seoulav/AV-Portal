# W-20260923-012 결과 — 공개 베타 로컬 검토본

2026-09-23 사용자 승인 범위에서 338개 로컬 목록의 일부만 공개 후보로 선별하고, 원본 없이 실행되는 정적 검토 화면을 만들었다. **외부 공개·외부 미리보기·자동 배포·결제·공개 후보 데이터의 GitHub 업로드는 하지 않았다.** 기획 [PR #27](https://github.com/Seoulav/AV-Portal/pull/27)은 MERGED, 병합 SHA `9e14b313bed3c5a2ef5a2e47abd1316ebc0792e4`다. 그 커밋의 READY 명세를 읽고 별도 `codex/W-20260923-012-public-beta-preview` 워크트리에서 수행했다.

## 실제 선별

- 기준 원본: 338개, SHA-256 `E0028FF1684F282712591DE3C846BEF28635AEAF5C6890D2FBF270391097960D`. 작업 전후 해시 동일.
- 로컬 정적 데이터: 장비 25개, 서비스 0개, 브랜드 14개, 제조사 HTTPS 제품 웹페이지 링크 25개. 기존 ID에 없는 신규 옵션은 0개.
- 보류 313개: 모델 본문 확인 근거가 없는 307개(서비스 2개 포함), 기록은 있으나 모델 대응이나 제품별 페이지 성격이 불명확한 6개. 보류 항목을 판매 중지·미지원으로 해석하지 않는다.
- 출력 필드: `brand`, `product`, `categories`, `kind`, `official_links`만 허용. 원본 행·제품 ID·별칭·내부 메모·공급처·개인 경로·보조 출처·사진/PDF와 직접 제공 파일은 출력하지 않았다.
- 개별 항목·URL 및 보류 사유는 **Git 제외 로컬** `outputs/public-beta-W-20260923-012/review.md`에만 기록했다. 결정표와 정적 배포 폴더도 같은 Git 제외 위치에 있다. 이 PR에는 실제 항목값·실제 선별 데이터가 없다.

## 구현과 선택 이유

기존 내부 앱은 변경하지 않고 독립 정적 화면을 만들었다. 별도 서버나 영구 DB 없이 선별 결과 `catalog.json`만 읽으므로 개인 PC 원본이 없어도 배포 묶음이 실행된다. 생성기는 원본 해시와 명시적 결정표를 대조하며, 선택된 원본의 모델 본문 확인 기록과 제조사 HTTPS 호스트·제품 페이지 형식만 허용한다. PDF 확장자와 쿼리 다운로드도 거부한다. 원본 전체나 비공개 필드가 우연히 전달되지 않도록 다섯 필드만 새 객체로 투영한다. 출력은 저장소 루트의 Git 제외 `outputs/`에서만 허용하고, 동일 이름의 기존 출력 폴더는 생성 전에 거부한다.

화면에는 브랜드·제품명 검색, 브랜드 선택과 복수 카테고리 필터, 장비/서비스 탭, 제조사 웹페이지 링크가 있다. 서비스 0건은 빈 상태 설명을 표시한다. AV System Builder·LED Configurator는 기존처럼 외부 링크다. 최종 Schema·DB와 신규 옵션, 사진/PDF 권한 판정은 이번 범위가 아니다.

## 검증

- Node.js v24.18.0에서 실제 로컬 목록·자료대장 경로를 지정한 `npm test`: 35/35 통과. 공개 데이터 투영·유출 방지·잘못된 링크 거부·독립 파일 생성·로컬 서버 경로 제한을 합성 테스트로 확인했다.
- `npm run verify:resources:local`: 제품 338, 원본 행 342, 대장 제품 338, 자료 455, 관계 557 통과.
- 생성된 JSON 25개 모두 다섯 허용 필드만 있고 각 항목 공식 HTTPS 링크 1개임을 검사했다. 기준 원본 해시가 결정표와 일치한다.
- 로컬 `127.0.0.1:4174` 화면에서 장비 25개·14브랜드·서비스 0개, 제품/브랜드 검색, 브랜드 선택, 두 카테고리 동시 선택(17개), 서비스 빈 상태를 확인했다. 첫 화면의 브랜드 기본값 결함을 발견해 수정한 뒤 25개 정상 표시를 재확인했다.
- 로컬 결정표·검토표·정적 데이터는 Git 제외로 유지하고 수행 PR 파일 목록에 없는지 확인한다. 독립 리뷰의 출력 경로·쿼리 다운로드·기존 출력 보호 지적을 수정하고 회귀 테스트로 확인했다. 외부 접속이나 업로드는 실행하지 않았다.

## 배포 후보와 한계

[Cloudflare Pages Direct Upload](https://developers.cloudflare.com/pages/get-started/direct-upload/)는 생성된 정적 폴더를 Git 연동 없이 수동 업로드할 수 있다. [공식 Pages 무료 플랜은 $0](https://www.cloudflare.com/developer-platform/products/pages/)이다. [사이트 한도](https://developers.cloudflare.com/pages/platform/limits/)는 20,000개 파일·개별 25 MiB이고, 대시보드 드래그 업로드는 별도 1,000개 파일 제한이 있다. 이 검토본은 파일 4개, 최대 파일 8 KiB 미만이다. 프로젝트를 실제 만들면 주소는 일반적으로 `https://<프로젝트명>.pages.dev/` 형식이다. **프로젝트명·주소는 아직 정하지 않았고 실제 외부 주소도 없다.** Direct Upload 프로젝트는 뒤에 Git 연동 방식으로 전환할 수 없으므로 최종 배포 승인 때 운영 방식을 다시 정한다. 링크 대상 제조사 사이트의 후속 변경·접근성과 제품 상세 사양은 이번 테스트가 보증하지 않는다.

장애: 없음. 남은 제한: 선별 25개와 공식 링크를 사용자가 최종 검토해야 하고, 보류 313개를 추가하려면 모델별 공식 근거를 보강해야 한다. W-006/PR #10과 옵션 19행은 베타 제작의 선행 조건이 아니며 여전히 별도 과제다. 외부 게시와 권한 확대는 별도 명시 승인 전 진행하지 않는다.
