# W-20260925-002 → Work 인계

- 상태: REVIEW
- 구현 브랜치: `codex/W-20260925-002-navigation-ui`
- 기준 main: `0028d22e3f432e8240f74373f5077da7405ae3cd`
- 공개 배포: 수행하지 않음

## 구현 결과

- Home을 검색 우선 탐색 화면으로 변경했다. 초기에는 제품 목록을 숨기고 통합 검색, 상위 카테고리 6개, 제조사 선택, AV System Builder와 LED Configurator 링크를 먼저 보여준다.
- 검색·카테고리·제조사 선택 후 결과 수, 하위 카테고리, 제조사, 자료 상태, 정렬과 초기화가 나타난다. URL에 탐색 상태를 기록하고 상세에서 돌아올 때 스크롤 위치를 복원한다.
- 검색 제안은 제품·제조사·카테고리로 나누고 키보드로 이동할 수 있다. 공개 Library와 공개 Group 1 상세 데이터만 검색한다.
- Product Detail은 제품 이미지/요약/Quick Documents/공식 페이지 중심의 상단과 개요·특징·스펙·연결 단자·관련 제품·문서·출처·검증 내비게이션을 사용한다.
- 연결 단자는 Desktop 연속 표와 Mobile 신호 그룹별 아코디언으로 바꿨다. 옵션·조건·검토 필요만 강조하고 전체 출처·검증·적용 조건은 별도 펼침에 보존한다.
- 자체 리뷰 후 검토 중 값의 검색 노출, 검색 제안과 기존 필터 충돌, checkbox 재생성에 따른 키보드 포커스 손실을 고쳤다. 확정된 공개 값만 검색에 보강하고 같은 facet 목록은 DOM을 교체하지 않는다.

## 데이터 보호

- 공개 제품 27개와 Group 1 상세 JSON 5개의 값은 변경하지 않았다.
- Features 8/8/8/7/8, Specifications 27/16/16/13/16, I/O 14/15/11/4/9, Quick Documents 4칸을 유지했다.
- 비공개 Excel, 업체 정보, 내부 메모, 로컬 경로, 권한 미확인 이미지/PDF를 추가하지 않았다.
- 상위 카테고리는 탐색용 매핑이며 기존 원본 카테고리를 변경하지 않는다.

## 검증 결과

- `npm test`: 65 pass, 1 skip, 0 fail. skip은 비공개 338개 원본 환경 변수가 없을 때의 기존 조건부 검사다.
- `node beta/verify-pages.mjs`: 27개 장비, 5개 공개 상세, 비공개 자산 누출 없음.
- 정본/생성물 6쌍 일치, JavaScript 구문 검사, `git diff --check` 통과.
- 1440×900, 768×900, 390×844에서 가로 넘침 0.
- 음향 카테고리 12개, `Dante` 검색 DM7 1개, 제조사/URL/뒤로가기 스크롤 복원 확인.
- 5개 상세 데이터 개수와 Desktop 표 행 수, DM7 Mobile 6개 그룹·15개 행을 확인했다.

## 사용자 검토 자료

Git 제외 경로 `outputs/w025-review/`에 다음 PNG를 만들었다.

1. `01-desktop-home.png`
2. `02-mobile-home.png`
3. `03-audio-category.png`
4. `04-search-results.png`
5. `05-product-detail-top.png`
6. `06-connectors-desktop.png`
7. `07-connectors-mobile.png`

## 후속 사항

- 사용자가 화면을 확인하기 전 main 병합과 GitHub Pages 배포를 하지 않는다.
- 전체 제품의 별칭·기능·연결 규격 검색 확대는 공개 가능한 구조화 데이터가 준비된 뒤 별도 작업으로 진행한다.
- 관련 제품 추천은 관계 데이터와 노출 기준이 마련되기 전까지 추가하지 않는다.

## 사용자 화면 검토 후 보정 (2026-09-25)

- 초기 Home은 Hero 검색 하나만 표시하며 Header 검색은 숨긴다. 검색·카테고리·제조사 결과에서는 반대로 Header 검색만 표시한다. 모바일도 동일하다.
- 이미지 없는 제품 결과는 낮은 정보형 카드로 표시한다. 음향 결과 12개 카드는 1440px에서 각 163px이며 빈 이미지 영역을 만들지 않는다.
- 연결 단자는 물리 포트 총수로 오해되지 않게 `15개 연결 항목`, 그룹별 `4종` 형식으로 표시한다. 표는 포트 수와 채널·신호를 별도 열로 나눴고, 숫자로 확인되지 않은 포트 수는 `—`로 남긴다.
- DM7 표는 `구분 | 연결 단자 | 방향 | 포트 수 | 채널·신호 | 규격·조건` 6열과 15행이다. 제조사 원문을 바꾸지 않고 화면 표시만 XLR 3핀 입력/출력·XLR 4핀 입력·XLR 디지털 오디오·D-sub 15핀으로 정리했다. 원본 값과 검증 정보는 하단 펼침에 남는다.
- 조건이 없는 행에는 배지를 표시하지 않는다. 실제 조건, 옵션, 카드/모듈 필요, 확인 필요만 보조 표시한다.
- 390px에서는 첫 그룹인 오디오 4종만 기본으로 열고 나머지는 닫는다. 그룹 칩은 가로 스크롤되며 큰 단자 카드를 만들지 않는다.
- 새 사용자 검토 화면은 Git 제외 경로 `outputs/w025-review/08-corrected-desktop-home.png`부터 `12-corrected-dm7-connectors-mobile.png`까지다.
- 공개 데이터 JSON과 제품 범위는 바꾸지 않았다. PR #70에서 계속 검토하며 사용자 승인 전 main 병합과 Pages 재배포를 하지 않는다.
