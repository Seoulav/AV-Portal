# W-20260924-009 → Codex 인계 — 공개 상세 진입 경로 결함과 CI 검증 범위 수정

- 작성 담당·일시: Work, 2026-09-24 (KST)
- 작업 ID: W-20260924-009
- 명세 경로·상태: [Work/작업/W-20260924-009.md](../작업/W-20260924-009.md) / **DRAFT**
- 기준 main 커밋: `d782d4e585be57ed42801fd4cba95b02cd96348f`
- 기획 브랜치: `claude/new-session-oay7ox`
- 기획 PR·확인 시점의 상태: 생성 후 실제 URL과 상태를 채운다
- 병합 증거: **미병합**
- 사용자 승인 근거: 2026-09-24 사용자가 Claude 독립 검증 결과를 받은 뒤 "chatgpt codex에 제안해줘"라고 지시. 제안 문서 작성까지만 승인된 상태이며 **구현·병합·공개 배포 승인은 아직 없다.**
- Codex 착수 판정: **Codex 착수 불가 — 명세 DRAFT, 사용자 구현 승인 없음, main 병합 전**

## 선행 조건과 접근 가능 자료

| 조건/자료 | 확인 결과와 근거 | 미충족 시 담당자·다음 조치 |
|---|---|---|
| 사용자 승인 | 제안 문서 작성만 승인. 구현·배포 승인 없음 | 사용자 → 이 명세를 읽고 구현 착수와 배포 범위를 승인 |
| 선행 작업 병합 | 해당 없음. W-20260924-007(#40)·008(#42·#43·#44)은 `d782d4e` 시점에 이미 main 반영됨 | 없음 |
| READY 명세와 인계서의 main 반영 | 미병합. 현재 `claude/new-session-oay7ox`에만 존재 | Work → 기획 PR 병합 후 상태를 READY로 갱신 |
| 필요한 로컬 자료·개발환경 | Node.js 20 이상만 필요. 비공개 원본(`hkkim/`, `outputs/`)과 npm 패키지 설치 불필요. `package.json`에 dependencies·devDependencies 모두 없음 | 없음 |
| 중복 구현·진행 중 PR | `Work/지시서.md`와 `Work/작업/`을 대조해 ID `W-20260924-009`가 미사용임을 확인(사용 중: 001~008). 이 세 결함을 다루는 진행 중 작업 기록 없음 | Codex → 착수 직전 열린 PR을 재조회 |
| 공개 URL 실측 | **미확인.** 검증 세션의 네트워크 정책이 `seoulav.github.io`로의 egress를 차단(`403 to CONNECT`) | Codex → 병합 후 실제 공개 URL에서 재확인하고 기록 |

근거 문서 [AV-PORTAL-CLAUDE-REVIEW-2026-09-24.md](AV-PORTAL-CLAUDE-REVIEW-2026-09-24.md)를 같은 PR에 포함한다. 이 문서와 명세에는 비공개 원본·개인 PC 절대 경로·공급처·단가가 없다.

## 구현 범위와 제외 범위

- 구현할 사용자 동작·변경 경로
  - `beta/site/detail/app.js` — 쿼리 없는 진입 시 Library 이동, 오류 안내에 Library 링크, 렌더 완료 후 해시 재보정, 모델 모듈 import의 캐시 갱신 쿼리
  - `beta/site/detail/index.html` — `?v=` 값 갱신
  - `beta/site/detail/styles.css` — 출처 앵커의 `scroll-margin-top` 보완
  - `.github/workflows/pages.yml` — 배포 전 `npm test`
  - `Work/지시서.md` — 진행 기록
- 제외 범위
  - 공개 제품 데이터 JSON 5개, `catalog.json`, Library 27개 항목의 값 변경
  - `beta/verify-pages.mjs`의 고정 해시·파일 목록 assert 완화
  - `prototype/` 전체
  - 명세 "미결 사항"의 명암비·`target="_blank"`·`detail-links.js` 테스트·Library 캐시 쿼리 (별도 작업 ID 필요)
  - 최종 Product Schema·DB·Storage·Migration, RTCOM Logic, Group 2 추가
- 유지할 정책
  - 공개 아티팩트에 파일을 추가하지 않는다. `verify-pages.mjs`의 `detail/` 파일 목록 assert가 그대로 통과해야 한다.
  - Pages 업로드 경로는 `beta/site` 하나를 유지한다.
  - 제조사 사양·재게시 권한을 새로 추정하지 않는다. `VERIFIED`/`FOUND`/`READY`/`PARTIAL`/`MISSING`/`REVIEW REQUIRED`/`CONFLICTED` 구분을 유지한다.

## 완료 조건과 검증 방법

명세의 완료 조건 체크리스트를 `Work/지시서.md`로 옮겨 실행 결과를 체크한다.

**계획된 검증 (Codex가 수행)**

```text
node beta/verify-pages.mjs
npm test
```

`beta/site` 전체를 쿼리 문자열과 함께 제공하는 정적 서버에서 Desktop 1440px·Mobile 390px로 다음을 확인한다. `beta/serve.mjs`는 초기 Library 파일만 제공하므로 사용하지 않는다.

- `/detail/`, `/detail/?product=` → Library 이동
- `/detail/?product=nope`, `?product=BRC-AM7`, `?product=..%2F..%2Fcatalog` → 거부 + Library 링크
- `/detail/?product=brc-am7#io` 직접 진입과 새로고침 → `#io .section-title`이 상단 약 100px
- `#source-HS` 직접 진입 → sticky 내비게이션에 가리지 않음
- 섹션 탭 6개 클릭 회귀 없음
- 5개 상세의 Quick 4칸 / Features 8·8·8·7·8 / Specs 27·16·16·13·16 / I/O 14·15·11·4·9, I/O 즉시 표시, 가로 넘침 0
- Library 장비 27·서비스 0, 상세 링크 5개

측정은 `getBoundingClientRect().top` 실측치로 기록한다. 수행하지 않은 검증은 통과로 적지 않는다.

**이미 수행한 검증 (2026-09-24, Claude 독립 검증, main `d782d4e`)**

- `node beta/verify-pages.mjs` 통과, `npm test` 42 통과·1 건너뜀(`AV_PORTAL_PRODUCT_LIST_PATH` 미설정)·0 실패
- Chromium 실측으로 결함 1·2 재현. `?product=brc-am7#io` 직접 진입 시 1440px `scrollY=1611` / `#io .section-title` 상단 +1167px, 390px `scrollY=1616` / +1945px. 섹션 탭 클릭은 6개 모두 `headTop=100`으로 정상
- 공개 JSON 5개의 전체 키 집합에 provenance·원본 행 ID·공급처·내부 메모 없음. 공개 스크립트 4개에 `innerHTML`·`eval` 등 0건
- **미검증:** 실제 공개 URL, 제조사 사양의 진위, 사진·PDF 재게시 권한

## 착수 판정과 다음 조치

현재 **Codex 착수 불가**다. 미충족 조건과 조치는 다음과 같다.

| 미충족 조건 | 담당자 | 조치 |
|---|---|---|
| 사용자 구현 승인 없음 | 사용자 | 명세를 읽고 구현 착수 승인. 공개 배포 승인은 별도로 명시 |
| 명세 상태 DRAFT | Work | 승인 확인 후 READY로 변경 |
| 명세·인계서 main 미병합 | Work | 기획 PR 병합, 병합 SHA 기록 |

문서 병합만으로 제품 기능이나 공개 배포가 승인되지 않는다.

## 사용자에게 전달할 결과

기획 PR의 실제 상태를 재조회해 명세·인계서 경로, 확인 시점, PR URL, 병합 SHA, 착수 판정을 보고한다. 이 인계서는 **병합 전 스냅샷**이다. 위 세 조건이 모두 충족되기 전에는 Codex 구현 프롬프트를 제공하지 않는다.
