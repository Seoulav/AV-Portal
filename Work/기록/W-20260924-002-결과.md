# W-20260924-002 — BRC-AM7 로컬 시안 결과

- 수행 [PR #34](https://github.com/Seoulav/AV-Portal/pull/34)는 OPEN/DRAFT이며 PR #33 기획 브랜치를 대상으로 한다. GitHub 변경 파일 10개는 시안 코드·실행 기록뿐이고 사진·베타·Pages 파일은 없다. 최신 Pages Actions 실행 #35880194401과 main `888b035a`는 불변이다.
- 기준: PR #33 head `3217646c5ba1b612c0520cc4492bb7a0bcc4462d`; main `888b035aafaeb9eb0472abcc2ec4e42a565b578f`.
- 이번 사용자의 명시적 승인: 한 제품 로컬 Product Detail 구현·검증. PR #33의 기존 DRAFT/구현 금지는 이 좁은 범위에 한해 최신 지시로 대체. PR #33과 수행 PR을 main에 병합하지 않는다.
- 실행: 저장소 루트에서 `node prototype/brc-am7/fetch-images.mjs` 후 `node prototype/brc-am7/serve.mjs --port 4185`; 회사 PC 브라우저 `http://127.0.0.1:4185/`.
- 구현: 9개 요청 영역, 네 역할 공식 사진의 로컬 Gallery·확대·사진별 공식 URL/표시 크기/권한 추적, Quick Documents 6개 카드, Features 8·사양 27(11그룹)·I/O 14, 공식 출처 6개와 상태 구분. 추가 공식 문서 두 개는 Documents에만 둠. MISSING 4종은 링크 없이 표시.
- 검증: 실제 브라우저에서 4역할 로드·전환과 Rear 개별 출처 표시, 확대와 Esc 닫기/초점 복귀, Rear 바로가기, Quick Documents, 수량 8/27/14, 출처·검토 상태 확인. 390×844, 768px, 1440px에서 문서 수평 넘침 없음. 누락 문서 링크 0개, 외부 이미지 `src` 0개. 로컬 HTTP는 시안 4파일·사진 4개 200, `Work/`와 `outputs/` 경로 404. 실제 입력 `npm test`: 35/35. `node --check` 통과. 이미지가 없으면 해당 썸네일을 비활성화한다. 사진 4개는 `.gitignore`의 `outputs/`에 저장돼 커밋 대상에서 제외.
- 코드 경계: `beta/site`와 Pages workflow 수정 없음. 기존 공개 후보 25개와 데이터는 불변. 공개용 이미지·PDF·CAD 원본 없음. 루프백 서버만 실행.
- 남은 문제: 사진/PDF/CAD 재게시 허가 미확인, C1 확장 줌 충돌, 일부 펌웨어 조건, CAD 정밀 검증. 로컬 이미지 폴더는 Git으로 전달되지 않으므로 다른 PC에서는 공식 사진을 다시 로컬에 받아야 한다. 디자인과 정보 밀도는 사용자 화면 검토 전 확정하지 않는다.

## 디자인 1차 수정 (2026-09-24)

사용자 검토 의견에 따라 기존 시안의 5개 영역만 다듬었다. 문서 종류를 카드 아이콘과 키운 제목으로 바로 구별하고 언어·검증 상태는 보조 줄로 이동했다. Header 한국어 설명은 화면에서 최대 3줄로 제한하되 Overview의 같은 원문과 콘텐츠 JSON은 그대로 둔다. Specifications 값은 더 크게, VERIFIED는 연하게 표시하고 REVIEW REQUIRED/CONFLICTED와 출처·조건은 유지한다. I/O는 원래 14개 카드를 신호별 6개 그룹(4/4/2/2/1/1)으로 재배열했다. Power의 PoE++ 안내는 LAN RJ-45의 기존 조건·출처를 참조하며 새 I/O 사실이나 중복 카드를 만들지 않는다. 섹션 탭은 스크롤 위치에 따라 현재 영역을 표시하며 앵커의 부드러운 이동과 모바일 가로 탭을 유지한다.

검증: 로컬 서버 `http://127.0.0.1:4185/`에서 1440×900과 390×844 실제 화면 확인. Quick Documents 6·Features 8·Specifications 27·I/O 14, 모바일 헤더 정확히 3줄, Header/Overview 원문 일치, MISSING 문서 링크 0, 각 뷰포트의 문서 가로 넘침 0. I/O 탭 선택 후 섹션 제목이 sticky 탭 아래에 보이고 `aria-current=location`이 I/O로 바뀜을 확인. `node --check prototype/brc-am7/app.js` 및 `git diff --check` 통과. `npm test` 결과 35개 중 34 통과, 1개는 비공개 로컬 목록 환경변수 미설정으로 건너뜀, 실패 0. 공개용 베타·콘텐츠 JSON·사진 원본은 수정하지 않았고 GitHub Pages 재배포와 main 병합은 하지 않는다.

남은 검토: 사용자 화면 평가, 사진/PDF/CAD 재게시 권한, 확장 줌 C1 충돌, 일부 펌웨어 조건과 CAD 본문 정밀 확인. 다른 제품 확장은 사용자 검토 전 진행하지 않는다.