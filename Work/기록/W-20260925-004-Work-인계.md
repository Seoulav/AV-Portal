# W-20260925-004 → Work 인계 — Product Detail 이미지 중심 개선과 상태 표시 정정

## 완료한 범위

- Group 1 공통 Product Detail의 모델명·이미지·설명·문서 읽기 순서를 정리했다.
- Quick Documents 4슬롯과 실제 확보·검토·없음 건수를 분리했다.
- 모바일 Header 검색을 접고 펼치는 버튼으로 바꾸고, 7개 sticky 탭의 활성 항목 자동 노출을 추가했다.
- 모바일 연결 단자 그룹 칩을 두 줄까지 배치하고 첫 그룹만 기본 펼침 상태로 유지했다.
- 이미지가 없는 네 제품은 큰 빈 갤러리와 대체 사진을 만들지 않는다. BRC-AM7은 기존 CC0 범용 픽토그램을 실제 사진과 명확히 구분한다.

## 검증

- Group 1 문서 집계: BRC-AM7 `2/1/1`, DM7 `1/2/1`, Ki Pro GO2 `2/0/2`, PT-MZ17K `3/0/1`, Rally Bar `1/1/2` (`확보/검토/없음`).
- 1440px·390px의 다섯 상세에서 가로 넘침 0, 탭 7개·표시 패널 1개·문서 4슬롯을 확인했다.
- I/O `14/15/11/4/9`, DM7 모바일 그룹 6개·첫 그룹만 펼침·15행을 확인했다.
- Home 카테고리 6개·제조사 15개·초기 결과 숨김과 음향 결과 12개를 확인했다.
- `npm test` 67 pass·1 skip·0 fail, `node beta/verify-pages.mjs`, 정본/생성물 7쌍 대조, JavaScript 구문 검사와 `git diff --check`를 통과했다. skip은 비공개 338개 목록 경로가 없을 때의 기존 조건부 검사다.

## 남은 일

- `hkkim/제품이미지_업로드/`에는 README 외 제품 사진이 없다. 사용자가 정확 모델 사진과 공개 조건을 제공하면 역할·모델 일치·권리를 확인한 뒤 별도 작업으로 연결한다.
- Group 1 네 제품의 제조사 이미지 재게시 권한과 이미지 역할 검토 상태는 그대로다. 화면에 사진이 보이지 않는 것은 결함이 아니라 현재 공개 정책의 결과다.
- main 병합과 GitHub Pages 배포는 사용자 승인 전 수행하지 않는다.

## 독립 리뷰 보정

- 모바일 첫 진입의 활성 탭 자동 스크롤을 사용자 탭 선택·키보드 이동 때만 실행하도록 제한했다. DM7 390px 무해시 진입은 `scrollY=0`이며 모델명이 화면 안에 유지된다.
- 이미지 없는 제품도 모델명 직후에 간결한 `제품 이미지 준비 중` 패널과 공식 출처·역할·재게시 상태를 표시한다. 이 패널은 DM7 Desktop에서 약 115px 높이이며 큰 빈 갤러리는 만들지 않는다.
- 보정 후 `npm test` 67 pass·1 skip·0 fail, Pages 아티팩트 검사, 정본/생성물 대조, JavaScript 구문 검사와 `git diff --check`를 다시 통과했다.
- 직접 `#sources` 진입도 세로 위치와 별개로 탭바만 수평 이동하도록 보완했다. 390px에서 출처·검증 탭 전체가 탭바 안에 표시되며, 일반 첫 진입은 `scrollY=0`과 개요 탭 위치를 유지한다.

## 다음 Work 기획 입력

- XDM 관련 제품은 일반 유사 제품 추천이 아니라 `정확한 XDM 프레임 → 호환 입력 카드/출력 카드` 관계로 정의한다.
- 프레임 모델별 카드명, 슬롯, Revision, 장착·동시 사용 조건과 공식 문서 페이지를 준비한다. 카드 명칭 충돌이나 호환 미확정 항목은 일반 추천에서 제외하거나 검토 중으로 분리한다.
- Product Detail의 관련 제품 renderer 연결은 호환표와 공개 대상 카드가 승인된 뒤 별도 작업으로 작성한다.
