# W-20260923-003 → Work 인계 — 첫 로컬 탐색 앱 다음 단계

- 인계 작성일: 2026-09-23
- 기준 main: `cfaa0942a3bba5f567c4ba482284b33f72bfca04`
- 완료 명세: [W-20260923-003](../작업/W-20260923-003.md)
- 진행·검증 기록: [Work/지시서.md](../지시서.md)
- 명세 PR [#3](https://github.com/Seoulav/AV-Portal/pull/3), 구현 PR [#4](https://github.com/Seoulav/AV-Portal/pull/4), 병합 기록 PR [#5](https://github.com/Seoulav/AV-Portal/pull/5): 모두 MERGED
- 기존 [W-20260922-002](../작업/W-20260922-002.md)는 별도 DRAFT다. 이번 인계로 READY가 되지 않는다.

## 실제 완료와 한계

Node.js 로컬 앱이 실행 시 지정한 제품 JSON을 읽어 338개 고유 제품을 한 번씩 표시하고, 342개 원본 행과 복수 카테고리를 유지한다. 브랜드·제품명·별칭 검색, 브랜드·복수 카테고리 필터, 공식·보조 URL, 제조사 직접 제공 참조, 브랜드 미식별·자료 링크 미확보 표시를 구현했다. Builder·LED는 외부 링크다. 실제 목록으로 `npm test` 14/14와 `npm run verify:local` 1/1이 통과했고, 브라우저에서 주요 검색·필터·출처 사례를 확인했다. 로컬 목록과 hkkim 원본은 GitHub에 포함하지 않았다.

이 앱은 이 PC의 `127.0.0.1:4173`에서 동작하는 첫 탐색 화면이다. 현재 URL의 실시간 유효성, 자료가 해당 모델에 적용되는지, 사양의 정확성, 공개 준비 여부를 판정하지 않는다. 제품별 구조화 사양·I/O·이미지 상세, 자료 종류별 Documents 화면, Compare, 기존 RTCOM Configurator 연결·저장·검증·BOM, 관리·게시, 서비스 배포는 아직 구현되지 않았다. 최종 Schema·DB·Architecture는 결정하지 않았다.

[기존 인계](../기획/WORK_HANDOFF.md)의 RTCOM 28개 장비·시리즈 항목은 과거 RTCOM Library 범위다. 이번 338개 브랜드·제품 목록 전체와 같은 집계가 아니다. [MVP 문서](../기획/PORTAL_MVP_SPEC.md)의 단계·기능 제안과 2026-09-23 사용자가 승인한 첫 목록 구현을 대조해 다음 범위를 정한다.

## 다음 작업 후보 — Work가 먼저 구체화

**우선 후보: 제품별 자료 수집대장 읽기 전용 연결.** 로컬 `outputs/research-master-20260923/resource-library/`에 제품별 자료 기록 `resource-register.json`과 현 앱 호환 목록 `AV_Portal_Product_List.resources.json`이 있다. 로컬 `hkkim/처리기록.md`에 따르면 자료 120건·제품 연결 154건, 참조 없는 제품 215개이며, 호환 목록의 공식 링크 연결 제품은 103개에서 104개가 됐다. 이는 이번 인계 시 앱에 반영·재검증한 수치가 아니라 로컬 자료 작업의 기록이다. 현재 앱은 호환 목록을 파일 경로로 교체해 실행할 수 있지만 수집대장의 자료 종류·언어·상태·복수 제품 연결을 표현하지 못한다.

Work는 우선 자료 표시 기능의 최소 범위를 새 작업 ID의 DRAFT로 설계한다. 예: 제품 상세에서 제품 페이지·사양서/카탈로그·매뉴얼·제어 자료·기타를 구분하고, 공식·제조사 직접 제공·보조 출처, 언어, 열람/모델 적용 확인 수준을 표시한다. 자료 하나가 여러 제품에 연결될 수 있어야 한다. 검색 색인만 확인하고 본문을 열지 못한 후보, RTCOM ZIP의 파일명만 일치한 후보는 원문 확인 또는 모델 적용 완료로 승격하지 않는다. 로컬 파일 공개·다운로드, 업로드·편집, DB 도입은 이 후보의 기본 범위로 넣지 않는다. 제품 ID 338개·원본 행 342개 유지, 원본 불변, 비공개 경로 비노출을 검증 조건으로 제안한다.

**후속 후보:** 자료가 확인된 제품부터 상세 사양·I/O·전후면 이미지와 근거를 표시하고, 공통 탐색·Documents·동일 수준 제품 비교를 단계적으로 만든다. 현행 RTCOM 코드와 비민감 저장 파일 예시가 확보되면 Configurator 진입·복귀 및 기존 기능 보존 작업을 별도 명세로 분리한다. 관리·게시와 배포는 공개 범위·운영 접근 방식이 정해진 뒤 진행한다. 세부 SKU·색상·주파수 대역·호환성 전수 확정을 위 첫 자료 표시 작업의 착수 조건으로 두지 않는다.

## Work가 접근할 수 있는 것과 없는 것

이 파일, `Work/지시서.md`, 완료 명세, 앱 코드·README, PR은 GitHub main에서 읽을 수 있다. 반면 `hkkim/`, `outputs/research-master-20260923/`, 원본 ZIP과 제품 JSON·수집대장은 이 PC의 Git 제외 로컬 자료다. ChatGPT Work가 GitHub 연결로 main을 읽더라도 로컬 파일 내용까지 받은 것은 아니다. Work는 아래 구조 요약으로 DRAFT를 시작할 수 있다. 개별 자료의 실제 값과 변환 규칙은 확정된 것으로 가정하지 말고, 구현 시 Codex가 로컬 원본과 다시 대조하도록 완료 조건에 적는다. Work가 사전에 반드시 확인해야 하는 구체적 원본 정보가 있으면 필요한 항목만 사용자에게 요청한다. 로컬 원본을 GitHub에 올려 해결하지 않는다.

현재 로컬 수집대장 `resource-register.json`의 **구조 요약**(필드명·상태명만 공유, URL·원본 경로·파일 내용 제외)은 다음과 같다. 파일 SHA-256은 `7330C4F6A9753B9A31A8A9D78944C04899E95590962C190DA2CC2982E3C6E335`다. 이는 최종 Product Schema가 아니라 후속 명세를 위한 현재 입력 계약 후보이며, 구현 시작 시 원본과 다시 대조한다.

| 배열 | 현재 필드 |
|---|---|
| `products` (338) | `product_id`, `brand`, `product`, `categories`, `folder`, `resource_ids`, `next_collect`, `priority`, `method` |
| `resources` (120) | `resource_id`, `title`, `authority`, `kind`, `url`, `local_reference`, `language`, `revision`, `verification`, `checked_on` |
| `product_resource_links` (154) | `product_id`, `resource_id`, `applicability` |

현재 상태값은 `authority`: `MANUFACTURER_DIRECT`/`OFFICIAL`/`SUPPLEMENTAL`, `kind`: `CATALOG`/`MANUAL`/`MANUAL_INDEX`/`PRODUCT_PAGE`/`REFERENCE`/`UNCLASSIFIED`, `verification`: `CONTENT_VIEWED`/`FILENAME_MATCH_BODY_UNREAD`/`INHERITED_NOT_RECHECKED`/`REFERENCE_ONLY`/`SEARCH_INDEX_ONLY_OPEN_FAILED`, `applicability`: `CANDIDATE`/`EXACT_MODEL`/`EXACT_MODEL_INDEX`/`EXACT_MODEL_ON_SERIES_PAGE`/`EXACT_MODEL_SECTION`/`FILENAME_CANDIDATE`/`UNASSESSED`다. 언어 값은 현재 `ko`/`en`이며, 비어 있는 값은 별도 미확인으로 유지한다. `folder`와 `local_reference`는 로컬 전용 경로이므로 공개 화면·응답에서 그대로 노출하지 않는다.

hkkim의 `AV_PORTAL_COMPANY_PC_HANDOFF/`는 처리기록상 무결성 확인 후 상세 반영 대기다. Work는 기존 기획과의 차이를 확인하되, 자료 수령 자체를 구현·스키마 승인으로 해석하지 않는다. 브랜드 미식별 4건과 한국 공식 자료 우선순위에 필요한 언어·Revision·적용 모델 확인도 남아 있다. RTCOM 웹 조사는 하지 않는다. 정식 공개에는 [MVP 자료 요건](../기획/PORTAL_MVP_SPEC.md)에 적힌 공식 페이지, 사양서, 매뉴얼, 전면·후면 이미지의 출처·적용 모델 검토가 필요하다. 338개를 일괄 공개 준비 완료로 간주하지 않는다.

## Work의 다음 실행과 사용자에게 돌려줄 결과

1. 최신 main과 열린 PR을 확인하고 위 문서·로컬 접근 한계를 대조한다.
2. 다음 한 작업의 최소 범위와 데이터·검증 근거를 새 `Work/작업/<ID>.md`에 **DRAFT**로 작성한다. W-20260922-002는 목적이 다르므로 자동 전환하지 않는다.
3. 사용자 승인이 필요한 제품 기능 범위나 공개 정책 결정은 명확히 표시한다. 승인 없는 DRAFT를 READY로 바꾸지 않는다. 독립적으로 승인된 준비 작업이 있다면 별도 ID로 분리한다.
4. 사용자에게 새 명세 경로·PR 상태·Codex 착수 가능 여부·정말 필요한 자료/결정을 한 번에 보고한다. 승인과 선행 조건을 충족한 READY 명세만 main에 병합한다.

앞으로 Codex는 작업을 끝낼 때 [인계 템플릿](../템플릿/Work-인계.md)에 따라 다음 인계 파일을 작성하고 구현 코드·진행 기록과 같은 PR에 포함한다. 이 파일은 완료 작업의 인계 기록이며 Work의 미래 명세를 대신하지 않는다.
