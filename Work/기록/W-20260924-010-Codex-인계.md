# W-20260924-010 → Codex 인계 — 공개 상세 자산의 원본 동기화

- 작성 담당·일시: Work, 2026-09-24 (KST)
- 작업 ID: W-20260924-010
- 명세 경로·상태: [Work/작업/W-20260924-010.md](../작업/W-20260924-010.md) / **READY**
- 기준 main 커밋: `c8c4bc217c3f4c5de4c5e8e5d605d197c3aabd4f`
- 기획 브랜치: `claude/new-session-oay7ox`
- 기획 PR·확인 시점의 상태: 이 문서를 포함한 PR. 병합 후 상태를 결과 보고에 기재
- 병합 증거: 병합 SHA는 병합 직후 사용자 결과 보고에 기재. Codex는 착수 전 `git fetch origin main`으로 확인
- 사용자 승인 근거: W-20260924-009의 2026-09-24 승인 범위 내 정정. 새 기능·데이터 변경 없음
- Codex 착수 판정: **착수 가능 — W-20260924-009와 같은 브랜치·같은 PR에서 함께 수행**

## 선행 조건과 접근 가능 자료

| 조건/자료 | 확인 결과와 근거 | 미충족 시 담당자·다음 조치 |
|---|---|---|
| 사용자 승인 | 충족. W-009 승인 범위 내 정정 | 없음 |
| 선행 작업 | W-20260924-009와 동시 수행. 별도 선행 없음 | 없음 |
| 명세의 main 반영 | 이 PR 병합으로 충족 | Codex → 착수 전 직접 확인 |
| 필요한 로컬 자료·개발환경 | Node.js 20 이상. 비공개 원본 불필요 | 없음 |
| 원본·생성물 대응 확인 | `beta/build-group1-pages.mjs:77-100` 직접 확인. 현재 두 쪽은 치환 규칙 적용 시 완전 일치 | 없음 |
| 기존 관행 확인 | `fd20219`(PR #42)가 `prototype/brc-am7/`와 `beta/site/detail/`를 같은 커밋에서 수정한 것을 확인 | 없음 |
| 동기화 검사 존재 여부 | **없음.** `tests/`·`scripts/`·`beta/` 전체에서 두 쪽을 대조하는 코드 0건 | 이번 작업에서 추가 |

## 구현 범위와 제외 범위

- 구현할 변경: `prototype/brc-am7/app.js`·`styles.css`(·필요 시 `index.html`)에 W-009 수정 반영, 원본↔생성물 동기화 테스트 추가
- 제외 범위: `beta/build-group1-pages.mjs`의 빌드 방식 변경(W-20260924-011 3단계), `prototype/brc-am7/content.json` 삭제, `prototype/group1/` 기능 변경
- 유지할 정책: 공개 아티팩트에 파일을 추가하지 않는다. `verify-pages.mjs`의 `detail/` 파일 목록 assert 통과

## 완료 조건과 검증 방법

명세의 체크리스트를 `Work/지시서.md`의 W-009 진행 영역에 함께 옮겨 기록한다.

```text
npm test
node beta/verify-pages.mjs
```

새 동기화 테스트는 빌드가 쓰는 치환 규칙을 재사용하거나 같은 값을 공유해야 한다. 규칙을 테스트에 손으로 옮겨 적으면 빌드 변경 시 함께 틀어진다.

프로토타입 회귀는 `node prototype/brc-am7/serve.mjs`로 확인한다. 프로토타입에서는 `?product=` 없는 진입이 기존대로 `content.json`을 사용해야 한다. W-009가 공개본에서 제거하는 fallback을 프로토타입에서까지 없애면 프로토타입이 깨진다.

**이미 수행한 확인 (2026-09-24, main `c8c4bc2`)**

- `beta/build-group1-pages.mjs`의 복사·치환 대상 6쌍 확인
- 치환 규칙 적용 후 `prototype/brc-am7/app.js`와 `beta/site/detail/app.js`가 완전히 일치함을 확인
- 동기화 검사가 저장소 어디에도 없음을 확인

## 착수 판정과 다음 조치

착수 가능하다. W-20260924-009와 같은 브랜치 `codex/W-20260924-009-detail-entry`, 같은 PR에서 수행한다. 별도 PR로 나누면 두 변경이 따로 병합될 때 공개본과 원본이 어긋난 상태가 main에 남는다.

## 사용자에게 전달할 결과

W-009 PR의 상태와 함께 보고한다. 동기화 테스트가 추가됐는지, 프로토타입 회귀 확인을 수행했는지를 구분해 적는다.
