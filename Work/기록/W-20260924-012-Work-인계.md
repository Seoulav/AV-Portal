# W-20260924-012 → Work 인계 — 상세 화면 정보 구조

- 기준 main: `d7b3f80e9fd0655e75616c3947c7471c2ae4d5ee` ([READY 명세](../작업/W-20260924-012.md), 기획 PR #49 병합 후).
- 구현 브랜치: `codex/W-20260924-012-techdata-detail`.
- 사용자 지시: 제공한 TechDataPS 모바일 제품 상세 캡처의 형태로 AV Portal의 공개 상세를 정리한다.

## 적용

- 공개 상세 5개가 공통 화면을 사용한다. 모바일에서 제품명·제조사·영문/한글 설명 다음 핵심 문서 4종 2×2를 배치하고, 특징은 번호가 붙은 단일 목록으로 노출한다. 섹션 링크는 간결한 밑줄형으로 바꿨다. 데스크톱은 설명과 문서를 나란히 표시한다.
- 현재 공개 가능한 제품 사진은 0장이므로 큰 빈 갤러리를 만들지 않고 이미지 확인·게시 권한 미확인 상태를 짧게 표시한다. 사진이 실제 사용 가능한 로컬 시안에서는 큰 갤러리가 제품명 앞에 나온다. 타사 캡처·제품 사진·PDF는 올리지 않았다.
- 제품 정보, 카테고리, 검증 상태와 I/O·추가 문서·제조사 출처는 남겨 두었다. 제품 JSON 5개, Library catalog 27개, 외부 도구 링크와 공개 경계 검사는 변경하지 않았다.

## 검증

- `node beta/verify-pages.mjs` 통과, `npm test` 42 pass/1 skip/0 fail, JS 구문 및 Git diff 검사 통과.
- 브라우저 390px에서 상세 5개 문서 4칸·2열, Features 8/8/8/7/8, Specs 27/16/16/13/16, I/O 14/15/11/4/9와 가로 넘침 0. 1440px 상세 5개 가로 넘침 0. BRC-AM7 `#io` 직접·재진입 약 +100px, `#source-HS` 약 +100px, 활성 탭 확인.
- 로컬 임시 단색 JPEG로 갤러리 4역할 전환·확대·Esc를 검증했다. 이 테스트 파일은 Git 밖에 두었고 제품 사진으로 사용하지 않았다.
- 독립 코드 리뷰에서 발견한 키보드 skip-link, 이미지 확대 hover, 섹션 탭 focus 대비 문제를 수정했다.

## 다음 Work 입력

- 열린 PR #47의 W-011은 DRAFT다. 이 작업은 상세 5개의 시각·정보 순서만 다뤘으며 Library 트리·URL·데이터 계약·추천 제품은 다루지 않았다. W-011은 이번 실제 화면 결과와 사용자 의견을 반영해 별도로 확정한다.
- PR #47의 W-010은 `prototype/brc-am7/` 원본과 `beta/site/detail/` 생성물 전체가 다음 빌드에도 일치하도록 하는 별도 정정이다. 이번에는 CSS를 양쪽 동일하게 하고 W-012 HTML/JS 변경을 함께 적용했지만, 기존 W-009의 원본/생성물 차이까지 해결하지 않았다. 다음 그룹 빌드 전에 W-010 완료 상태를 확인한다.
- 공개 이미지·PDF 재게시 권한은 여전히 미확인이다. 이번 화면은 이를 확정 상태처럼 보이지 않게 한다. hkkim 원본·비공개 목록은 이번 작업에 사용하거나 업로드하지 않았다.
