# W-20260925-007 Work 인계

## 완료 범위

- 최신 Product Detail 요약형 UI와 공개 JSON을 유지하면서 사용자용 출처 코드·목록·근거 링크를 제거했다.
- `출처·검증` 탭을 `검증 상태`로 변경하고 사양·연결 단자의 현재 검증 상태를 JSON에서 자동 집계한다.
- 검증 상태 탭에는 요약, 검토 중인 항목, 작은 상태 범례만 남겼다.
- 삭제된 `#source-XX` 앵커 주소는 검증 상태 탭 상단으로 이동하는 fallback으로 보존했다.
- Home과 Product Detail Footer의 사용자 노출 build·commit 문자열을 제거하고 내부 배포 메타데이터 파일은 유지했다.

## 검증

- `npm test`: 81 pass·1 skip·0 fail. skip은 로컬 비공개 목록 조건부 검사다.
- `node beta/verify-pages.mjs`, 정본/생성물 7쌍, JavaScript 구문, diff 검사를 통과했다.
- 공개 Product JSON 5개는 고정 SHA-256과 Git diff 모두 무변경이다.
- 5개 제품의 Features `8/8/8/7/8`, Specs `27/16/16/13/16`, 연결 단자 `14/15/11/4/9`, Quick Documents 4칸과 기존 배지 수를 유지했다.
- 1440·768·390px에서 7개 탭·표시 패널 1개, 출처 해시 링크 0개, JSON 집계 일치, 가로 넘침·콘솔 오류·검증 탭 명암비 미달 0건을 확인했다.
- PT-MZ17K 연결 단자는 VERIFIED 0건을 `검증 완료 항목 없음`으로 명시한다.
- 최신 UI baseline 대비 `#sources` 화면 높이는 5개 제품 모두 1440px·390px에서 감소했다.

## 남은 일

- 구현 결과는 Draft PR #83에서 검토한다: https://github.com/Seoulav/AV-Portal/pull/83
- 사용자 검토 후 병합 여부를 결정한다. 이번 승인에는 GitHub Pages 배포가 포함되지 않는다.
- 출처 데이터 자체는 공개 JSON에 그대로 남아 있다. 제조사 원본을 재검토하고 출처 코드를 보완하는 일은 별도 작업이다.
- 로컬 hkkim 자료와 비공개 원본은 읽기·변경·Git 추가하지 않았다.
