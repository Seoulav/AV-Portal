# W-20260925-014 — 삼성전자(16개) 매뉴얼 링크 조사 기록

작성: 2026-09-26, Claude Code 세션(Work/Codex 역할 동일 세션 대행)

## 배경

`AGENTS.md` 우선순위: 삼성전자 제품은 1순위 에스큐브랩, 2순위 삼성전자. 에스큐브랩(`samsungsmartsignage.co.kr`)은 사용자가 모바일에서 직접 확인해도 "접근 제한(Access is restricted)" 화면만 나와 이 세션과 무관하게 차단되어 있음을 확인, 2순위(삼성전자)로 진행했다.

## 방식

카탈로그의 기존 `official_links` 3건(LH85QMCEBGCXKR, LH98QECEDGCXKR, LH43QHCEBGCXKR)이 이미 `https://www.samsungsvc.co.kr/download/view?code=<모델코드>&...` 형태의 삼성 공식 지원/다운로드 페이지였다. 이 URL 패턴(`code=<모델코드>`)을 나머지 13개 모델에도 적용해 각각 `curl`로 실제 200 응답을 확인했다.

## 결과 16/16

전부 `https://www.samsungsvc.co.kr/download/view?code=<모델코드>` 형태. 기존에 이미 이 페이지를 가리키던 3건은 전체 쿼리스트링(prd1DepNm 등 브레드크럼용 파라미터 포함)이 약간 다르지만 같은 페이지를 가리키므로 문제없다.

`manufacturer-hosts.mjs`에 `Samsung: ['samsung.com', 'samsungsvc.co.kr']` 추가.

## 검증

- 16개 URL 모두 `curl -o /dev/null -w '%{http_code}'`로 200 확인(일부는 첫 시도에 `Recv failure: Connection reset by peer`가 있었으나 재시도로 200 확인 — 일시적 네트워크 문제로 판단)
- `node beta/verify-pages.mjs`: 254개, baseline 25개 해시 불변, PASS
- `npm test`: 100 tests, 99 pass · 1 skip · 0 fail

## 남은 일

없음. 삼성전자 16개 전부 완료.
