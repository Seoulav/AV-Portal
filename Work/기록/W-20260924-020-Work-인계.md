# W-20260924-020 Work 인계 — 이미지 중심 Product Detail 로컬 시안

## 결과

- BRC-AM7 로컬 미리보기만 사용자 제공 흰색·파란색 레이아웃에 맞춰 갤러리 중심 2열로 변경했다. 모바일에서는 헤더→갤러리→문서 순서다.
- 구현 [PR #64](https://github.com/Seoulav/AV-Portal/pull/64)는 Draft이며 미병합이다.
- 공식 Sony 사진 Main·Front·Rear·Perspective 4장을 **로컬 무시 경로** `outputs/brc-am7-detail/images/`에서만 읽는다. 이미지 원본, 공개 JSON, Pages 파일은 변경·업로드하지 않았다.
- 로컬 주소: `http://127.0.0.1:4211/` (실행 중인 해당 PC에서 확인). 재실행: `node prototype/brc-am7/fetch-images.mjs` 후 `node prototype/brc-am7/serve.mjs --port 4211`.
- `npm test`: 46 pass, 1 skip, 0 fail. `node beta/verify-pages.mjs`: 통과. Chromium 1440/390px에서 이미지 4장 로드, Rear 전환·확대·Esc, 가로 넘침 없음, 8/27/14 콘텐츠·문서 4칸 확인.

## 경계와 다음 판단

- 제조사 사진 재게시·핫링크 권한은 **REVIEW REQUIRED**. 공개 사이트에서 사진이 보이도록 바꾸는 작업은 하지 않았다.
- 공개 생성물 동기화는 자동 승인 검토에서 공개 사이트 변경 위험으로 거절됐다. 현재 시안은 프로토타입 서버에서 주입하는 `local-visual.css`로 격리했다. 이 거절을 우회해 동기화하지 않는다.
- 사용자의 디자인 시안 평가를 받고, 공개 사진의 사용 허가 또는 제조사 공식 페이지로 보내는 대안 중 선택한다. 이후 공통 Product Detail 5개로 확대할지 별도 범위를 정한다. 공개 적용 전에는 사진 바이너리·업체 내부 자료가 PR/Pages에 없음을 다시 검증한다.
