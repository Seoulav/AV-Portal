# W-20260923-013 결과 — 공개 베타 GitHub Pages 배포

- 사용자 승인: 2026-09-23 GitHub Pages 외부 공개, 기존 W-012 공개 범위 유지.
- 기준 main: `ec9c536674204446f3c5e288d95135165a82dff0`; 구현 브랜치 `codex/W-20260923-013-github-pages`.
- 공개 데이터: 기존 선별 장비 25개, 서비스 0개, 브랜드 14개. `brand`, `product`, `categories`, `kind`, `official_links` 다섯 필드만 담는다. 보류 313개와 미확정 옵션 19행은 추가하지 않았다.
- 데이터 동일성: W-012 검토본과 SHA-256 `A0CC18F8AAA8F8DA8F495FD629DB5EE060EEB5B5F93008C79A0EFCF8F8A2DD26` 일치.
- 구현: 기존 정적 화면 3개는 수정하지 않고 승인된 `catalog.json`만 `beta/site`에 추가했다. GitHub Actions가 이 네 파일만 Pages에 올리고 사전에 파일·데이터 불변성을 검사한다.
- 로컬 검증: 실제 로컬 목록을 지정한 `npm test` 35/35 통과. `node beta/verify-pages.mjs`에서 네 파일, 25개 장비, 다섯 필드, 중복 없음, HTTPS 공식 링크, 승인 스냅샷 해시 확인.
- 공개 누출 경계: Pages 아티팩트에 원본·결정표·내부 운영 파일 경로가 없다. 새 JSON은 허용 키만 포함하며 원본 행·공급처·메모·private provenance·로컬 경로·직접 제공 파일·키를 포함하지 않는다.
- 배포: 수행 PR #31 MERGED (`2da3ca22bef87e4d06ca75249fc35941fdfa2381`), [Actions 실행 #35879374484](https://github.com/Seoulav/AV-Portal/actions/runs/35879374484) success. 실제 주소: [https://seoulav.github.io/AV-Portal/](https://seoulav.github.io/AV-Portal/).
- 장애: 없음. 제한: 외부 제조사 링크의 향후 유효성은 이번 배포로 보장되지 않으며, 보류 313개와 옵션 19행은 별도 공개 판정이 필요하다.

기존 W-012 공개 보류 기록은 당시 상태로 유지하며, 이번 사용자 승인에 따라 이 승인 스냅샷만 공개한다.

## 실제 공개 주소 검증 — 2026-09-24

브라우저에서 첫 화면 25개 장비·14개 브랜드·서비스 0개를 확인했다. DM7 검색은 1건, Epson 브랜드 필터는 4건, 오디오+영상 복수 카테고리는 17건이며, 필터 초기화 후 25개 장비와 25개 공식 HTTPS 링크가 표시됐다. 서비스 보기는 0건 안내를 표시했다. 390×844 모바일 화면에서 첫 화면과 목록이 정상 표시되고 가로 넘침이 없었다. 새로고침 후에도 같은 주소에서 25건이 다시 로드됐다.

실제 서버 응답: `/`, `app.js`, `styles.css`, `catalog.json` 모두 HTTP 200. 원격 JSON은 25개 `equipment` 항목, 허용 다섯 키만 포함, SHA-256 `A0CC18F8AAA8F8DA8F495FD629DB5EE060EEB5B5F93008C79A0EFCF8F8A2DD26`로 로컬 승인본과 일치하며 비공개 표식 검사에서 검출되지 않았다. Pages의 `Work/지시서.md`, `outputs/`, `hkkim/`, `.git/config`는 HTTP 404다. GitHub PR #31의 파일 목록은 배포 설정·검사·공개 JSON·공동 기록 7개로 제한된다.