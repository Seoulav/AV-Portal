# AV-Portal 공동 작업 규칙

## 목적과 시작 순서

ChatGPT Work는 기획·설계·다음 작업 준비, Codex는 승인된 작업의 구현·검증·진행 기록을 담당한다. GitHub `Seoulav/AV-Portal`의 `main`에 병합된 문서와 코드가 공동 기준이다. 채팅 기록만으로 인계하지 않는다.

매 작업 시작 시 다음 순서로 읽는다.
1. 이 파일과 [모델 운영 정책](MODEL_ROUTING.md)
2. [Work 안내](Work/README.md)와 [지시서](Work/지시서.md)
3. 배정받은 `Work/작업/<작업ID>.md` 및 그 문서가 지정한 기획 자료
4. [기존 인계 문서](Work/기획/WORK_HANDOFF.md)의 제품 범위와 보류 조건

사용자의 명시적 최신 지시를 우선한다. 이 규칙의 협업 절차는 과거 문서의 작업 위치·시작 절차를 대체하지만 제품 범위·데이터 정확성·제품 구현 승인 조건은 유지한다. 이미 승인된 범위의 통상적인 문서 작성과 구현을 매 단계 재승인받지 않는다.

## 파일별 작성 책임

| 위치 | 주 작성자 | 규칙 |
|---|---|---|
| `Work/기획/` | Work | 요구사항·조사·설계. 기존 자료와 미확정 정보를 구분 |
| `Work/작업/<작업ID>.md` | Work | 목표·범위·완료 조건·승인 근거·의존 작업 작성 |
| `Work/지시서.md` | Codex | 작업 시작, 체크리스트, 검증 결과, 장애, 다음 인계 기록 |
| 코드·테스트 | Codex | READY로 인계되고 승인된 범위만 구현 |
| `Work/기록/` | 기록을 만든 담당자 | 기존 기록 보존, 작업별 파일로 나누어 추가 |
| `AGENTS.md`, `MODEL_ROUTING.md`, 공통 템플릿 | 해당 변경 담당자 | 별도 범위로 검토하고 동시 수정 조율 |

Work는 Codex가 실행 중인 작업 명세와 `Work/지시서.md`를 수정하지 않는다. 다음 계획은 새 ID의 파일에 작성한다. Codex는 현재 작업의 진행 영역만 갱신하고 미래 기획 파일을 임의로 확정하지 않는다. 명세 변경이 필요하면 Work가 별도 변경 작업을 만들고 Codex가 적용 시점을 기록한다.

## 브랜치·워크트리와 병렬 작업

- `main`: 병합된 공동 기준. 직접 작업하지 않는다.
- `work/<작업ID>-<설명>`: Work의 기획 브랜치.
- `codex/<작업ID>-<설명>`: Codex의 구현 브랜치.
- 협업 규칙 등 문서 정비는 `docs/<설명>`도 허용한다.
- 두 채팅은 **서로 다른 체크아웃 폴더 또는 워크트리**에서 실행한다. 브랜치 이름만 나누고 같은 폴더를 공유하면 안 된다. 같은 브랜치를 두 워크트리에서 체크아웃하지 않는다.
- 수동 설정 예시는 [Work 안내](Work/README.md)를 따른다. Codex 앱이 관리하는 Worktree를 사용해도 된다. 워크트리 폴더는 저장소 바깥에 둔다.
- 시작 전 `git status --short`, `git branch --show-current`, `git remote -v`를 확인하고 `git fetch origin` 후 최신 `origin/main`을 기준으로 시작한다. 다른 사람의 변경이 있으면 덮어쓰지 않는다.
- 브랜치와 파일은 자동 동기화되지 않는다. 상대의 미병합 결과는 **push된 브랜치의 특정 커밋 또는 PR**을 읽고 잠정 정보로 표시한다. 로컬 미커밋 결과를 완료 사실로 취급하지 않는다.
- Work는 Codex가 A를 구현하는 동안 독립적인 B를 작성할 수 있다. A 결과에 의존하는 B는 DRAFT로 유지하고 A 병합 후 최신 결과를 반영한 뒤 READY로 만든다.
- `Work/지시서.md` 작성자는 동시에 한 Codex 작업만 맡는다. Codex 구현을 여러 개 병렬화해야 하면 작업별 진행 파일과 통합 담당자를 먼저 정하는 별도 규칙 변경이 필요하다.

## 인계와 상태

1. Work가 [작업 템플릿](Work/템플릿/작업지시서.md)을 복사해 고유한 `W-YYYYMMDD-NNN` ID로 DRAFT를 작성한다. 시작 전 원격 작업 목록·열린 PR을 확인해 ID 중복을 피한다.
2. 요구사항·완료 조건·의존성·필요 승인 근거를 채운다. 승인 대상인 제품 결정에 대한 승인이 없으면 READY로 바꾸지 않는다. 명세 PR이 병합됐다는 사실만으로 제품 구현 승인이 생기지 않는다.
3. Work는 [Codex 인계 템플릿](Work/템플릿/Codex-인계.md)으로 `Work/기록/<작업ID>-Codex-인계.md`를 작성하여 명세와 같은 `work/` 브랜치 PR에 포함한다. 승인·선행 조건이 충족된 명세만 READY로 표시하고 main에 병합한다. 이미 받은 승인은 날짜와 범위를 기록한다. 인계서에 작업 ID·명세 경로·기준 main SHA·기획 PR과 확인 시점의 상태·승인 근거·선행 조건·구현/제외 범위·검증 방법·로컬 자료 접근 한계를 적는다.
4. Codex는 해당 명세가 포함된 최신 `origin/main`에서 구현 브랜치를 만든다. `Work/지시서.md`에 작업 ID, 명세 경로, **명세를 읽은 커밋 SHA**, 구현 브랜치, 범위, 체크리스트를 기록하고 IN_PROGRESS로 전환한다.
5. 수행한 항목만 `[x]`로 바꾼다. 검증 명령·결과·실행하지 못한 이유를 적는다. 차단 시 BLOCKED로 표시하고 막히지 않은 범위는 계속한다.
6. 검증과 자체 리뷰를 마치면 REVIEW로 표시하고 PR을 만든다. PR 본문에 작업 ID·명세·검증·후속 사항을 포함한다. 코드·진행 기록과 `Work/기록/<작업ID>-Work-인계.md`를 같은 PR에 넣는다. 인계 파일은 완료한 범위·검증·남은 일·Work의 다음 기획 입력·로컬 전용 자료 접근 한계를 구분한다.
7. 원격 최신 `main`을 통합한 뒤 충돌·검증·필수 CI·리뷰 상태를 확인하고, 사용자가 승인한 병합 권한 범위 내에서 병합한다. 보호 규칙을 우회하거나 강제 push하지 않는다.
8. Work는 다음 계획 작성 전 병합된 구현 PR, 지시서의 결과·미완료·장애 및 해당 `Work/기록/<작업ID>-Work-인계.md`를 읽는다. `MERGED` 여부는 GitHub PR 상태가 기준이다. 병합 직전 커밋 안에서 미래 병합을 완료로 체크하지 않는다. 다음 문서 PR에서 PR URL·병합 SHA를 근거로 완료 기록을 보완할 수 있으며, 이는 활성 작업이 아닌 종료 작업의 기록에 한한다.

Work는 READY 명세의 실제 main 병합·승인·선행 조건을 모두 확인하고 미완료 작업일 때만 실제 값이 채워진 Codex 구현 프롬프트를 결과 보고에 제공한다. 부족한 조건이 있으면 **Codex 착수 불가**와 담당자·다음 조치를 적고 코딩 지시는 제공하지 않는다. 이미 완료된 작업은 재구현하지 않는다. GitHub 쓰기가 불가능하면 명세와 인계서의 대상 경로·Markdown 전문을 담은 문서 반영 전용 프롬프트 하나를 제공한다. [Work 안내](Work/README.md)의 상태 확인 절차를 따른다.

명세의 READY는 인계 가능 여부이고, 실행 상태(IN_PROGRESS/BLOCKED/REVIEW)는 지시서에 기록한다. 명세 파일을 실행 중 상태 변경용으로 반복 수정하지 않는다. 검증 완료와 GitHub 병합 완료는 구분한다.

## 충돌·중단·검증

동기화 전 자신의 변경을 커밋한다. `git fetch origin` 후 작업 브랜치에서 `git merge origin/main`으로 통합한다. 충돌 시 양쪽 작업 의도를 비교하며 진행 체크를 기계적으로 덮어쓰지 않는다. 의미 충돌이 해소되지 않으면 해당 항목을 BLOCKED로 남긴다. `reset --hard`, 타인 변경 삭제, 승인 없는 강제 push로 해결하지 않는다.

PR은 `git diff --check origin/main...HEAD`로 커밋된 전체 PR 변경을 검사하고 변경 범위에 맞는 검증을 수행한다. 커밋 전에는 `git diff --check`와 `git diff --cached --check`도 확인한다. 문서 변경은 링크·원문 보존·작업 규칙의 일관성을 확인한다. 앱 변경은 루트 README의 실제 실행·테스트 명령과 변경 범위에 맞는 검증을 수행하고 결과를 지시서에 기록한다. 수행하지 않은 테스트를 통과했다고 쓰지 않는다.

## 제품 기준과 현재 제한

- 개발 대상은 AV Equipment Library / RTCOM Configurator 기반 AV Portal이다. AV System Builder와 LED Configurator는 외부 링크만 유지한다.
- 제품 구현 착수, 최종 Product Data Schema·DB·JSON Schema 확정, Migration, 기존 RTCOM Logic 변경 등 기존 인계 문서의 보류 항목은 별도 승인 전 진행하지 않는다. 이번 협업 문서 구성 승인은 제품 구현 승인이 아니다.
- 제조사 사양을 추측하지 않는다. 정보 없음, 0, 미지원, 해당 없음을 구분한다. 자료 충돌과 미확정 상태를 보존한다.
- RTCOM 자료는 사용자가 직접 제공한 제조사 자료를 우선한다. RTCOM 공식 홈페이지의 제품 소개·특징 같은 기본 정보는 조사할 수 있다. 두 출처를 구분하고 충돌 시 임의로 덮어쓰지 않는다. 홈페이지 설명만으로 미확인 사양·호환성·사진/PDF 재게시 권한을 확정하지 않는다. 이 정책은 2026-09-23 사용자의 최신 정정부터 적용하며 과거 작업의 당시 조사 범위 기록은 보존한다.
- 제외 모델은 HS-88M-U, HS-88MX, HD-D104U, HD-D108U이다. HD-104U와 HD-108U는 혼동하지 않는다. 원본 제조사 자료는 증빙으로 보존한다.
- **공식 링크(제품 페이지·매뉴얼 등) 출처 우선순위.** 2026-09-25 사용자 지시로 적용:
  - 하만(Harman 계열: JBL·AKG·Crown·Soundcraft·dbx·Lexicon·Martin·AMX 등) 제품 — 1순위 테크데이타 PS 자료, 2순위 제조사
  - 삼성전자 제품 — 1순위 삼성전자, 2순위 에스큐브랩
  - 그 외 제품 — 1순위 공식 제조사 한글 사이트, 2순위 공식 제조사 글로벌 사이트
  - Powersoft·EAW 제품의 시방서·사양서(스펙시트) — 1순위 다산SR(`https://www.dasansr.com/sub/products/`, 국내 공식 대리점) 자료. 다산SR에 해당 자료가 없으면 기존에 조사한 제조사 자료를 그대로 쓴다.
  - 이 우선순위는 앞으로의 링크 조사(신규·보완 모두)에 적용한다. 2026-09-25 사용자 확인: 기존에 이미 채워진 삼성·하만 계열 링크는 소급 재조사하지 않는다. Powersoft·EAW는 예외로, 다산SR 자료가 있으면 이미 채운 것도 교체한다.
- 일반 작업은 기본 모델을 사용하고, 복잡한 분석·리뷰에 한해 `MODEL_ROUTING.md` 기준으로 상향을 검토한다.
- 비밀키·토큰·비공개 원본 자료를 임의로 커밋하지 않는다.
